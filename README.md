# translate
idnex.html为主页，server.py基于FastAPI书写包含调用硅基流动的千问模型，本代码没有提供秘钥，需要在.env文件中把秘钥赋值给OPENAI_API_KEY，开发的过程首先是写后端的代码，通过创建一个简单的chain，在prompt中写相应的要求，以及输出的格式，让模型按要求输出，之后写前端页面，前端页面通过接收后端的信息将获取到的内容进行渲染，输出在页面上。
